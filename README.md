```markdown
# nxt-hospital-skeleton-project

## Release: SR-2.0.0

- Release tag: `SR-2.0.0`
- Release date: 2025-12-05
- Summary: Minor updates and packaging for the SR-2.0.0 release. See changelog in the release notes (project repo).

> This repository is a deployment skeleton that composes frontend images (`pandanxt/*`), a backend API image, and stateful services (MySQL, Redis). See `.github/copilot-instructions.md` for developer guidance.

```
# nxt-hospital-skeleton-project